export default `
  enum Operator {
    # "=" equal
    EQ

    # ">" greater than
    GT

    # ">=" greater than or equal to
    GTE

    # "<" less than
    LT

    # "<=" less than or equal to
    LTE
  }
`
